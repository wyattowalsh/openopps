# OpenOpps v0.1 Acceptance Matrix

Status legend: `pass` means validated in this workspace. Post-v0.1 follow-ups are explicitly non-blocking for this deterministic release package.

| Area                             | Status | Evidence                                                                                                                             | Notes                                                                                                                                                                                                                       |
| -------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| OpenSpec contract                | pass   | `rtk npx -y @fission-ai/openspec@latest validate "prepare-v0-1-release" --strict`                                                    | Local v0.1 change validates strictly.                                                                                                                                                                                       |
| CLI status/doctor                | pass   | `tests/integration/openopps/test_cli.py`; `uv run pytest`                                                                            | `status` and `doctor` report database/cache/plugin status, coverage summary, route readiness, missing route metadata, setup next actions, and parseable JSON.                                                               |
| JSON cleanliness                 | pass   | `tests/integration/openopps/test_cli.py`; `uv run pytest`                                                                            | JSON tests use `json.loads`; plugin load output is captured as warnings instead of leaking into CLI JSON.                                                                                                                   |
| Cache core                       | pass   | `tests/unit/openopps/test_cache.py`, `tests/unit/openopps/test_http.py`; `uv run pytest`                                             | Deterministic keying, TTL, refresh bypass, conditional 304 reuse, stale-on-error visibility, in-flight duplicate suppression, namespace purge, and connection lifecycle are covered.                                        |
| Cache integration                | pass   | `src/openopps/http.py`, `src/openopps/route_probe.py`; `tests/unit/openopps/test_http.py`, `tests/unit/openopps/test_route_probe.py` | Shared JSON request helper is cached for source adapters, job providers, and route probes. Route-probe tests cover cache reuse and explicit refresh.                                                                        |
| Plugin core                      | pass   | `src/openopps/plugins.py`; `tests/unit/openopps/test_plugins.py`; `uv run pytest`                                                    | Entry-point loading, validation, output capture, failure isolation, config-backed disabled/allow-list behavior, conflicts, and JSON-safe status are covered.                                                                |
| Plugin provider wiring           | pass   | `src/openopps/providers/`; `tests/unit/openopps/test_plugins.py`; `tests/unit/openopps/test_registry.py`                             | Plugin source/job provider capabilities can be built and appear in the provider registry without overriding packaged adapters.                                                                                              |
| Plugin example/template          | pass   | `examples/plugins/minimal-openopps-plugin/`                                                                                          | Minimal `pyproject.toml` entry point and no-op source/provider/route/metadata/cache/CLI contribution template added.                                                                                                        |
| Deterministic examples           | pass   | `src/openopps/examples.py`; `tests/unit/openopps/test_examples.py`; `tests/integration/openopps/test_cli.py`                         | `examples seed` writes deterministic synthetic sources, boards, routes, jobs, plugins summary, and cache records.                                                                                                           |
| Provider coverage metrics        | pass   | `src/openopps/coverage.py`; `tests/unit/openopps/test_coverage.py`                                                                   | Board-level baseline, adopted v0.1, detect-only, unsupported/unknown provider metrics and enrichment completeness are covered.                                                                                              |
| Provider coverage audit snapshot | pass   | `src/openopps/coverage.py`; `tests/unit/openopps/test_coverage.py`; `goals/openopps-v0-1/release-smoke.md`                           | `providers audit` reports candidate-provider evidence and do-not-adopt rationales from persisted boards. Representative live source snapshots are an explicit post-v0.1 follow-up before publishing real-world percentages. |
| CLI stable/admin regrouping      | pass   | `src/openopps/cli.py`; `tests/integration/openopps/test_cli.py`                                                                      | Stable public surfaces remain focused on everyday workflows; low-level source, board-route, provider-route, cache purge, and DB maintenance commands live under `admin`.                                                    |
| Metadata preservation/enrichment | pass   | `src/openopps/enrichment.py`; `tests/unit/openopps/test_enrichment.py`; `tests/integration/openopps/test_cli.py`                     | `admin boards enrich` dry-runs by default and can promote preserved payload metadata into normalized board fields with `--apply` while keeping raw payloads intact.                                                         |
| Dry-run/apply diagnostics        | pass   | `tests/unit/openopps/test_route_probe.py`, `tests/unit/openopps/test_health.py`; `uv run pytest`                                     | Route probing and provider health remain dry-run by default and require apply-style flags for mutation.                                                                                                                     |
| Storage/export parity            | pass   | `tests/integration/openopps/test_storage_export.py`; `uv run pytest`                                                                 | Board/job list and export filter parity, deterministic key ordering, empty outputs, spreadsheet formula neutralization, nested JSON serialization, and legacy SQLite repair remain covered.                                 |
| Docs and README                  | pass   | `cd docs && pnpm data:generate`; `cd docs && pnpm types:check`; `cd docs && pnpm build`; `cd docs && rtk lint`                       | README and Fumadocs pages cover status, admin commands, cache, plugins, examples, provider coverage/audit, metadata enrichment, storage/export behavior, and v0.1 CLI-only wording.                                         |
| Full Python suite                | pass   | `uv run pytest`; `uv run pytest --cov=openopps --cov-report=term-missing`                                                            | `231 passed`; total coverage `90.37%`, above the configured 90% floor.                                                                                                                                                      |
| Docs type/build                  | pass   | `cd docs && pnpm types:check`; `cd docs && pnpm build`; `cd docs && rtk lint`                                                        | Docs generated metadata, TypeScript, production build, and lint checks pass.                                                                                                                                                |
| Generated artifact hygiene       | pass   | `.gitignore`; `git diff --check`                                                                                                     | `openopps.cache.db*` is ignored; generated cache DB was removed from the worktree.                                                                                                                                          |
| Deterministic release smoke      | pass   | `goals/openopps-v0-1/release-smoke.md`                                                                                               | Temp-DB smoke covered status, plugins, cache, examples seed, jobs list, jobs export, provider coverage, and provider audit JSON output.                                                                                     |

## Validation Log

```bash
uv run ruff check
uv run ty check
uv run pytest
uv run pytest tests/integration/openopps/test_storage_export.py -q
uv run pytest tests/integration/openopps/test_migrations.py -q
uv run pytest tests/unit/openopps/test_job_enrichment.py tests/unit/openopps/test_enrichment.py -q
cd docs && pnpm data:generate
cd docs && pnpm types:check
cd docs && pnpm build
cd docs && rtk lint
rtk npx -y @fission-ai/openspec@latest validate "prepare-v0-1-release" --strict
rtk git diff --check
OPENOPPS_DB_URL=sqlite:////tmp/openopps-v01-smoke/openopps.db uv run openopps examples seed --json
```

## Post-v0.1 Follow-up

- Generate representative persisted live source snapshots and publish measured real-world provider audit percentages. This is not a blocker for the deterministic CLI-only v0.1 release package.
